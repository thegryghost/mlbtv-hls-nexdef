#ifndef MLBTV_INC
#define MLBTV_INC
#include <stdint.h>

#define MAX_STR_LEN			128
//#define MAX_GAMES_PER_DAY	30
/*
struct mlb_game_struct
{
	uint8_t status;
	char event_id[MAX_STR_LEN];
	char event_datetime[MAX_STR_LEN];

	char home_contentid[MAX_STR_LEN];
	char away_contentid[MAX_STR_LEN];

	char home_tv_str[MAX_STR_LEN];
	char away_tv_str[MAX_STR_LEN];

	char home_code[4];
	char away_code[4];

	char home_name[MAX_STR_LEN];
	char away_name[MAX_STR_LEN];

};
typedef struct mlb_game_struct MLB_GAME_STRUCT;

struct mlb_games_root_struct
{
	char datetime[MAX_STR_LEN];
	MLB_GAME_STRUCT games[MAX_GAMES_PER_DAY];
};
typedef struct mlb_games_root_struct MLB_GAMES_ROOT_STRUCT;
*/

#endif

